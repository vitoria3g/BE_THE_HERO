import React from 'react';
import ReactDOM from 'react-dom'; //react com o DOM
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

//serviceWorker.unregister();
